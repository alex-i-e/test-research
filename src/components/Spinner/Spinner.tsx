import React, { FC } from 'react';

import styles from './Spinner.module.css';

const Spinner: FC = () => {
	return (
		<div className={styles.wrapper}>
			Spinner
		</div>
	);
};

export { Spinner };
